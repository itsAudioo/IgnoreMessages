name: Build/Release IgnoreMessages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    outputs:
      version: ${{ steps.version.outputs.version }}
      commit_msg: ${{ steps.commit.outputs.message }}

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get commit message
        id: commit
        run: |
          MSG="$(git log -1 --pretty=%B)"
          echo "message<<EOF" >> $GITHUB_OUTPUT
          echo "$MSG" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          echo "Commit message: $MSG"

      - name: Extract base version from CHANGELOG
        id: version
        run: |
          VERSION=$(grep -Po '^##\s*\[?v?\K([0-9]+\.[0-9]+\.[0-9]+)' CHANGELOG.md | head -n1)
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Check if build is needed
        id: check
        run: |
          MSG="${{ steps.commit.outputs.message }}"
          if [[ "$MSG" == *"[build]"* || "$MSG" == *"[release]"* ]]; then
            echo "run=true" >> $GITHUB_OUTPUT
          else
            echo "run=false" >> $GITHUB_OUTPUT
          fi

      - name: Stop if not requested
        if: steps.check.outputs.run != 'true'
        run: |
          echo "No [build] or [release] tag found — exiting."
          exit 0

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore dependencies
        run: dotnet restore IgnoreMessages.csproj

      - name: Build & Publish IgnoreMessages
        run: |
          dotnet publish IgnoreMessages.csproj \
            -c Release \
            -o build/publish

      - name: Create plugin zip with root folder IgnoreMessages
        run: |
          mkdir -p build/zips
          cd build/publish
          zip -r ../zips/IgnoreMessages.zip ./ -x "*.pdb" "*.xml" "*.deps.json"
          # Move contents into IgnoreMessages/ folder inside zip
          cd ..
          mv zips/IgnoreMessages.zip zips/tmp.zip
          cd zips
          zip -r IgnoreMessages.zip IgnoreMessages tmp.zip
          rm tmp.zip
        shell: bash

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: IgnoreMessages-build-${{ steps.version.outputs.version }}
          path: build/zips

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    if: >
      github.event_name == 'workflow_dispatch' ||
      contains(needs.build.outputs.commit_msg, '[release]')

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: IgnoreMessages-build-${{ needs.build.outputs.version }}
          path: build/zips

      - name: Compute release tag
        id: tag
        run: |
          BASE="v${{ needs.build.outputs.version }}"
          echo "tag=$BASE" >> $GITHUB_OUTPUT
          echo "prerelease=false" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          name: IgnoreMessages ${{ steps.tag.outputs.tag }}
          prerelease: ${{ steps.tag.outputs.prerelease }}
          body_path: release_body.md
          files: build/zips/*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
